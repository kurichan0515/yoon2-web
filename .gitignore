# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# AI tools (personal settings - not for sharing)
.kiro/
skills-lock.json

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
/out
/.next

# misc
.DS_Store
# .env は git-crypt で暗号化してgit管理するため、.gitignoreから削除
.env.local
.env.development.local
.env.test.local
.env.production.local

# git-crypt keys
.git-crypt-keys/
*.key

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript build cache
*.tsbuildinfo

# env backup
.env.backup

/src/generated/prisma

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/terraform.tfvars
terraform/.terraform.lock.hcl
